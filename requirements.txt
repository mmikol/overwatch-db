beautifulsoup4>=4.12
psycopg[binary]>=3.1
requests>=2.31

# Development
pytest>=7.0
pyflakes>=3.0        # unused imports and dead names

# Optional: embedded Postgres for local runs (--local-server)
# pgserver
